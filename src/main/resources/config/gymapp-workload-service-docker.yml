spring:
  data:
    mongodb:
      uri: mongodb://root:password111@mongo:27017/trainers_training_duration?authSource=admin
  activemq:
    broker-url: tcp://activemq:61616
    user: admin
    password: admin
    in-memory: false
    pool.enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://gymapp-eureka:8761/eureka
    register-with-eureka: true
    fetch-registry: true