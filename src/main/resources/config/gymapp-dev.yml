spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gymapp_local_dev
    username: ${LOCAL_AND_DEV_USER}
    password: ${LOCAL_AND_DEV_USER_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update